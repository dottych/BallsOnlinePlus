const letters = {
    a: 'KeyboardEvent'[(Math.tan(Math.PI/2)+'')[9]],
    b: 'atob'['KeyboardEvent'.substring(0,3).length],
    c: btoa(String.fromCharCode(114))[0],
    e: (Boolean(!0)+'')[Math.floor(Math.PI)],
    i: (typeof KeyboardEvent)[4+Math.sin(Math.PI/2)],
    l: (!1+'')[2],
    n: (typeof Math.PI)[Math.sin(0 / Math.PI)],
    o: (typeof !1)[Math.floor(Math.PI / 2)],
    s: (typeof 'atob')[0],
    ' ': "typeof 'string'"[6],
};